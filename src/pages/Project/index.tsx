import classNames from "classnames/bind";
import styles from "./Project.module.scss";
const cx = classNames.bind(styles);
const Project = () => {
    return <div className={cx("wrapper")}>Project</div>;
};

export default Project;
